.venv/
book/

*.iso
*.log
*.png
*.tgz
*kubeconfig.yaml
Chart.lock
.vscode/settings.json
bootstrap/argocd/githubsecret.env
bootstrap/root/.cephrc

# Talos configuration with secrets (base configs have tokens/certs)
/metal/talos-configs/
/talos-configs/
# Playbook talos-configs has generated base configs with secrets, but patches are safe
metal/playbooks/talos-configs/*.yaml
metal/playbooks/talos-configs/talosconfig

# Proxmox API credentials
metal/.env

# Talos image files (too large, download on demand)
metal/roles/talos_vm/files/*.raw
metal/roles/talos_vm/files/*.raw.zst
