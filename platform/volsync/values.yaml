# Enable metrics for Prometheus monitoring
metrics:
  service:
    enabled: true
    port: 8080
  # Disable authentication to allow Prometheus to scrape metrics easily
  disableAuth: true

# ServiceMonitor configuration for Prometheus
serviceMonitor:
  enabled: false
  interval: 30s
  scheme: http
  port: 8080

# Custom ServiceMonitor with proper authentication
customServiceMonitor:
  enabled: true

# Additional RBAC configuration to allow Prometheus to scrape metrics
rbac:
  create: true